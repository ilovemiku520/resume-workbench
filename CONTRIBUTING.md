# 参与开发

先阅读 [LICENSE](LICENSE) 并取得著作权人的事先书面许可；Star 不构成授权。获得许可后，可参与中文排版、模板导入、无障碍与测试的改进。

1. 安装 Node.js 22.13+，运行 `npm ci`。
2. 修改后执行 `npm run check`、`npm run test:e2e`。
3. 使用完全虚构的测试资料，禁止提交真实姓名、联系方式、照片、雇主内部资料、API Key、上传模板或浏览器存储。
4. 新模板使用 `shared/schema.ts` 中定义的受限 JSON 参数；不要增加任意脚本、远程资源或可执行 HTML。
5. AI 功能必须提供可审阅建议，保留原稿、事实来源与验证局限；模拟结果要明确标注。

报告问题请附复现步骤和已脱敏截图。不要把 API Key 或完整私人简历放入公开 Issue。涉及安全的问题请使用 GitHub 私密漏洞报告功能（若仓库已启用），不要公开可被滥用的敏感资料。
